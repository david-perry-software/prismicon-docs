Source: inline argument — 2026-09-08

I want to add a variant system to prism icons so there are multiple styles that can be used from the package. Ideally, it should be easy to modify code to swap between them.

There will be may variants in the future so make sure we have a system for adding and managing them.

The first variant I want to be based on different dimensional n-cubes with their corresponding namitations for representation in a 3d plane